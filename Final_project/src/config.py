# config.py - 物理与仿真参数配置
import numpy as np

# --- 物理参数 ---
C_BG = 1450.0      # 背景介质声速 (m/s)
C_NORMAL = 1550.0  # 健康肝组织声速
C_HARD = 1600.0    # 硬化区域声速

# --- 几何模型参数 (单位: m) ---
L_X = 0.1          # 计算域宽度
L_Y = 0.1          # 计算域高度 (为了多重网格方便，建议设为正方形)
CENTER_X = 0.05    # 同心圆中心 X
CENTER_Y = 0.04    # 同心圆中心 Y
R_INNER = 0.015    # 硬化区半径
R_OUTER = 0.025    # 健康区半径

# --- 声源参数 ---
SRC_X = 0.05       # 源位置 X
SRC_Y = 0.02       # 源位置 Y (位于内圆下方)
F0 = 2e6           # 中心频率 2MHz
A0 = 1e6           # 源强度

# --- 仿真控制 ---
# 默认网格尺寸 (Task A, B, C 使用, Task D 会覆盖此设置)
# 推荐使用 2^k + 1 的尺寸以适配多重网格
GRID_SIZE_DEFAULT = 129 

# 时间步长控制
# 隐式格式可以使用较大的 CFL，这里设置相对于显式 CFL 极限的倍数
DT_FACTOR = 2.0